//A program that reads 2 positive integers
//and prints the integers between them, one per line.
//written by MINGLANG XIE 22/3/2019

#include <stdio.h>


int main (void){
    int low;
    int high;
    
    printf("Enter lower: ");
    scanf("%d", &low);
    
    printf("Enter upper: ");
    scanf("%d", &high);
    
    int i = low + 1;
    
    while (i < high){
        printf("%d\n", i);
        i++;
    }
    
    return 0;
}        
